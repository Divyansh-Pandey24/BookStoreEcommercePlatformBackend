<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<!--
        PARENT: Spring Boot manages all library versions.
        You never worry about "which version of library X 
        works with Spring Boot Y" â€” parent handles it.
    -->
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.2.4</version>
		<relativePath />
	</parent>

	<groupId>com.booknest</groupId>
	<artifactId>auth-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>auth-service</name>

	<properties>
		<java.version>17</java.version>
		<spring-cloud.version>2023.0.1</spring-cloud.version>
		<lombok.version>1.18.38</lombok.version>
	</properties>

	<dependencies>

		<!--
            SPRING WEB
            Gives you: embedded Tomcat, @RestController,
            @GetMapping, @PostMapping, Jackson (JSON converter)
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>

		<!--
            SPRING SECURITY
            We use ONLY:
            - BCryptPasswordEncoder (hash passwords)
            - SecurityFilterChain (configure rules)
            - OAuth2 integration (Google login flow)
            
            We DISABLE:
            - Sessions (we use JWT instead)
            - Login page (we have React)
            - CSRF (not needed for REST APIs)
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<!--
            OAUTH2 CLIENT
            This single dependency makes Spring handle 
            the ENTIRE Google OAuth2 flow automatically:
            - Redirecting user to Google
            - Receiving the callback code
            - Exchanging code for Google access token
            - Fetching user profile from Google
            We only write what happens AFTER all that.
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>

		<!--
            SPRING DATA JPA
            - @Entity maps Java class to MySQL table
            - JpaRepository gives free CRUD operations
            - Hibernate generates all SQL
            You write zero SQL for basic operations
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<!--
            MYSQL DRIVER
            JDBC driver for MySQL connection.
            scope=runtime: needed to run, not to compile.
        -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!--
            EUREKA CLIENT
            Registers this service with Eureka on startup.
            Gateway finds auth-service by name "AUTH-SERVICE"
            without knowing port 8081 directly.
        -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>

		<!--
            LOMBOK
            Generates getters, setters, constructors at 
            compile time using annotations.
            @Data = all getters + setters + toString
            @NoArgsConstructor = empty constructor
            @RequiredArgsConstructor = constructor for 
                                       final fields only
        -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!--
            VALIDATION
            @NotBlank, @Email, @Size on DTO fields.
            Spring auto-rejects invalid requests before 
            they reach your service method.
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!--
            JWT â€” THREE parts always needed together:
            
            jjwt-api: the interfaces you code against
            (Jwts.builder(), Claims etc.)
            
            jjwt-impl: the actual implementation
            scope=runtime: only needed at runtime
            
            jjwt-jackson: reads/writes JWT payload as JSON
            scope=runtime: only needed at runtime
        -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.11.5</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
			<version>2.5.0</version>
		</dependency>

	</dependencies>

	<!--
        Controls ALL Spring Cloud library versions together.
        Never add version numbers to spring-cloud 
        dependencies above â€” this block handles them.
    -->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>-Dnet.bytebuddy.experimental=true -XX:+EnableDynamicAgentLoading --sun-misc-unsafe-memory-access=allow --add-opens=java.base/java.lang=ALL-UNNAMED --enable-native-access=ALL-UNNAMED -Xshare:off</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

    </plugins>
	</build>

</project>


